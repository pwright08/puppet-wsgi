## MANAGED BY PUPPET

# Backup <%= @service %> log files
<%= @logs_dir %>/*.log {
  missingok
  notifempty
  dateext
  su <%= @app_user %> <%= @app_group %>
  rotate 4
  <%= @logrotate_freq %>
  compress
}
