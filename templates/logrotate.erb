## MANAGED BY PUPPET

# Backup <%= @service %> log files
<%= @logs_dir %>/*.log {
  missingok
  notifempty
  dateext
  su <%= @app_user %> <%= @app_group %>
  rotate <%= @logrotate_rotate %>
  <%= @logrotate_freq %>
  compress
  copytruncate
}
