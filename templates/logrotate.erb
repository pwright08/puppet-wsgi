## MANAGED BY PUPPET

# Backup <%= @service %> log files
<%= @logging_path %>/*.log {
  missingok
  notifempty
  dateext
  dateformat %Y-%m-%d
  su <%= @app_user %> <%= @app_group %>
  rotate 4
  weekly
  compress
}
